using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class FlyingCharacter : MonoBehaviour
{
 /*  private Vector3 flyDirection;
    public static void Create(Transform prefab, Vector3 Spos, Vector3 flyDirection){
       Transform flyingBodyT = Instantiate(prefab, Spos, Quaternion.identity);
FlyingCharacter flyingCharacter =flyingBodyT.gameObject.AddComponent<FlyingCharacter>();
    flyingCharacter.Setup(flyDirection);
    }

    private void setup(Vector3 flyDirection){
this.flyDirection = flyDirection;
    }

private void Update(){
    transform.position += flyDirection * Time.deltaTime; 
}    */

}
